# Easy Calculator Android App

This is the Android version of Easy Calculator. It works offline.

## Build on a computer

Open the `android-app` folder in Android Studio and select **Build > Build APK(s)**.

The debug APK is created at:

`app/build/outputs/apk/debug/app-debug.apk`

GitHub Actions also builds an APK automatically after changes to this folder.
